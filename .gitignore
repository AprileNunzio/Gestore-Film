# Ambiente Python
.venv/
__pycache__/
*.pyc
*.pyo

# Dati generati a runtime (vivono in %APPDATA%, vedi app/core/paths.py;
# queste righe coprono solo il fallback usato in sviluppo se APPDATA manca)
settings.json
dati_recenti.json
cache_elite.db
file_registry.db
logs/
error_log.txt
.env

# Build (PyInstaller onefile: vedi Gestore_Film_Portable.spec; installer: vedi installer/installer.iss)
build/
dist/
installer_output/
*.spec.bak

# Binari vendored troppo grandi per git (~100MB l'uno): vedi README per come ottenerli
ffmpeg/bin/*.exe

# npm (wrapper senza dipendenze, ma per sicurezza se mai ne venissero aggiunte)
node_modules/

# Editor / OS
.vscode/
.idea/
Thumbs.db
.DS_Store
